# SMTP Configuration for sending student result emails
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=sprawdziany@example.com

# Next.js Configuration
NODE_ENV=development
NEXT_PUBLIC_WS_URL=/api/ws
HOST=0.0.0.0
PORT=3000
